# QuickMenu-Plugin
QuickMenu is an Unreal Engine editor plugin that adds a customizable radial pie menu (hotkey V). Menus are configured via node graphs in the Content Browser, with context-aware switching, submenus, and 15+ action types. Supports UE 4.27 to 5.7.
